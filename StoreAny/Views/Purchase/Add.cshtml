
@model StoreAny.Models.PurchaseModel

@{
    ViewBag.Title = "Add";
}

<h2>Add</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>PurchaseModel</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <select>
                @foreach(var e in Model.Customer)
                {
                <option value="e">text</option>
                }

            </select>
        </div>
        <div>
            <select>
                @foreach(var e in Model.Supplier)
                {
                <option value="e">text</option>
                }

            </select>
        </div>
        <div>
            <select>
                @foreach(var e in Model.ItemList)
                {
                <option value="e">text</option>
                }

            </select>
        </div>
       
        <input type="submit" value="Create" class="btn btn-default" />
    </div>
        </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
